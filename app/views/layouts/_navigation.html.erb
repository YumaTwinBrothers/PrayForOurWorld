<nav class="navbar navbar-default"
 style="height:60px;border: none; background: linear-gradient(41deg, purple 25%, orange 50%, rgba(255, 107, 0, 0.48)) fixed; position: fixed; width:100%; z-index: 9999;">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false" style="margin-top:15px; background:white">
        <span class="sr-only">navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="inori-icon">
        <% if logged_in? %>
          <%= link_to image_tag('inori.png'), chat_path %>
        <% else %>
          <%= link_to image_tag('inori.png'), root_path %>
        <% end %>
      </div>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right" style="font-weight: bold">
        <% if logged_in? %>
          <li class="dropdown">
            <div class="dropdown-toggle" style="text-decoration: none; color: #888888; background:none; padding:10px 10px 0 0" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.username.capitalize %>'s Profile <%= "Admin" if current_user.admin? %><span class="caret"></span></div>
            <ul class="dropdown-menu" style="background:white">
              <li><%= link_to "Edit your profile", edit_user_path(current_user) %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Log out", logout_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
